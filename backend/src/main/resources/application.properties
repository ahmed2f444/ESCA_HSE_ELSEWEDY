spring.application.name=esca-hse-api
server.port=${SERVER_PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://${DB_HOST:zephyr.proxy.rlwy.net}:${DB_PORT:17885}/${DB_NAME:railway}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Africa/Cairo}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:XpHdmtESQcZsfimIUuCwdqDcXutJYMhD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${DDL_AUTO:none}
spring.jpa.open-in-view=false
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.sql.init.mode=${SQL_INIT_MODE:never}
spring.sql.init.continue-on-error=true

spring.jackson.time-zone=Africa/Cairo
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5180,http://localhost:3000,http://localhost:3100,http://localhost:3200,http://127.0.0.1:5180}
app.security.enabled=${APP_SECURITY_ENABLED:false}
app.security.jwt-secret=${APP_JWT_SECRET:local-development-secret-change-before-deployment}
app.security.token-ttl-seconds=${APP_TOKEN_TTL_SECONDS:28800}
app.security.demo-users-enabled=${APP_DEMO_USERS_ENABLED:true}
app.demo-data.enabled=${APP_DEMO_DATA_ENABLED:false}
app.automation.client-id=${AUTOMATION_CLIENT_ID:esca-hse-automation}
app.automation.client-secret=${AUTOMATION_CLIENT_SECRET:local-automation-secret-change-me}
app.automation.token-ttl-seconds=${AUTOMATION_TOKEN_TTL_SECONDS:900}

