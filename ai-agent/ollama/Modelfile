FROM qwen2.5:7b-instruct-q4_K_M

# Large context window to hold full SQL result payloads from MySQL
PARAMETER num_ctx 8192

# -1 = unlimited output — model generates the full table/list without truncation
PARAMETER num_predict -1

# Use all 4 CPU cores on quad-core laptop (d3500 qc)
PARAMETER num_thread 4

# Deterministic, grounded responses — low temperature prevents hallucination
PARAMETER temperature 0.1
